apptainer {
        enabled = true
        runOptions = '-B /data/'
        envWhitelist = 'APPTAINER_BINDPATH, APPTAINER_LD_LIBRARY_PATH'
}

report.overwrite = 'true'
trace.overwrite = 'true'

process {
    executor  = 'slurm'
    errorStrategy = 'retry'
    maxRetries = 3
    maxErrors = '-1' 


        withLabel: 'injection' {
        cpus = 48
        memory = '360 GB'
        time = '2h'
        queue = 'short.q'
        maxRetries = 3
        }

        withLabel: 'pulsarx_parfold' {
        cpus = 2
        memory = '30 GB'
        time = '2h'
        queue = 'short.q'
        maxRetries = 3
        }

        withLabel: 'presto_parfold' {
        cpus = 2
        memory = '30 GB'
        time = '2h'
        queue = 'short.q'
        maxRetries = 3
        }

        withLabel: 'dspsr_parfold' {
        cpus = 2
        memory = '30 GB'
        time = '2h'
        queue = 'short.q'
        maxRetries = 3
        }
       
}